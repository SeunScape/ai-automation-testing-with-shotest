import { shortest } from "@antiwork/shortest";

shortest('Test all validation rules on the checkout form including required fields, email format, zip code validation');

shortest('Navigate through the entire checkout form using only keyboard (Tab and Enter keys) and verify all elements are accessible')

shortest("Verify the application properly handles and recovers from a failed payment attempt");